class PlayerUpgrade {
    constructor(upgradeName) {
        this.upgradeName = upgradeName;

    }

    buildButton(scene){
        const buttonContainer = new Phaser.GameObjects.Container();

    }
}